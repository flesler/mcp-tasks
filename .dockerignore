# Include only what we need: package*.json and dist/index.js
# Exclude everything else first, then include specific files

# Exclude all
*

# Include only what we need
!package.json
!package-lock.json
!dist/index.js 